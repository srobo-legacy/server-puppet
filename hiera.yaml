---
:backends:
  - yaml
:yaml:
  :datadir: /etc/puppet/hieradata
:hierarchy:
  # Per machine overrides where the content needs to be kept secret
  - "secrets/%{::fqdn}"
  # Per machine overrides which are under version control
  - "nodes/%{::fqdn}"
  # Shared default configuration
  - common
